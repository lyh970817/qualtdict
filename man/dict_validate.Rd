% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict_validate.R
\name{dict_validate}
\alias{dict_validate}
\title{Return Validation Findings for a Variable Dictionary}
\usage{
dict_validate(dict, quiet = TRUE)
}
\arguments{
\item{dict}{A Variable Dictionary returned by
\code{\link[qualtdict]{dict_generate}}.}

\item{quiet}{Boolean. If \code{TRUE}, suppress routine validation messages.
Defaults to \code{TRUE}.}
}
\value{
A list with stable components:
\itemize{
\item \code{validation_findings} - A data frame of Validation Findings for
Variable Dictionary consistency issues, including repaired variable names,
non-unique or unsafe final \code{variable_name} values, and level-label coding
findings. Each row carries a \code{severity}: \code{"definite"} when the finding
makes the affected export column uninterpretable or its identity
unreliable (the Export-blocking level-label codings, and inconsistent,
duplicate, or unsafe final \code{variable_name} values), \code{"suggestive"} when
the finding only reports something worth review while the column's data
and identity stay sound (a repaired \code{variable_name}; a level-label
finding, such as a gapped level run, none of whose tripped tests is
Export-blocking).
\item \code{level_label_pairs} - A data frame containing the unique level-label
pairings.
}
}
\description{
Validate Variable Dictionary consistency and return a stable validation
object. Validation Findings report repaired \code{variable_name} values,
non-unique or unsafe Dictionary Variable Names, and level-label coding
issues. The validation result is a consistency screen, not proof that the
source Qualtrics metadata is correct.
}
\details{
\code{dict_validate()} is total: it reports every Validation Finding and never
errors, so it can be used to survey the damage in a defective Variable
Dictionary. Use \code{\link[=assert_dict_valid]{assert_dict_valid()}} when you want the Export-blocking
subset to error instead.
}
\examples{
dict <- data.frame(
  response_column_id = c("QID1", "QID2"),
  variable_name = c("q1", "q2"),
  label = c("Yes", "No"),
  level = c("1", "2")
)
class(dict) <- c("qualtdict", class(dict))

validation <- dict_validate(dict)

}
